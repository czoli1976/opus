# Prompt for a local coding agent

Paste the block below into a local agent (Claude Code, Cursor, etc.) running on
your machine. It will clone the branch, build the WASM, and start the server.

---

```
You are setting up a local web demo: "Opus 1.6.1 WASM + Vonage Video API".
The code lives in the GitHub repo czoli1976/opus on branch
claude/dazzling-goldberg-cd6axh, in the subdirectory opus-wasm-demo/.

Goal: build the WebAssembly Opus module and run the demo server locally.

Do the following, stopping to report any error you can't resolve:

1. Get the code (skip clone if the repo already exists locally):
     git clone https://github.com/czoli1976/opus.git
     cd opus
     git fetch origin claude/dazzling-goldberg-cd6axh
     git checkout claude/dazzling-goldberg-cd6axh
     cd opus-wasm-demo

2. Verify the toolchain. The prebuilt WASM is committed, so a rebuild is
   OPTIONAL. Only rebuild if I ask, or if public/opus-wasm.wasm is missing.
   To rebuild you need: Node.js >= 18, CMake >= 3.13, and Emscripten (emcc).
     - If emcc is missing, install it:
         git clone https://github.com/emscripten-core/emsdk.git ~/emsdk
         (cd ~/emsdk && ./emsdk install latest && ./emsdk activate latest)
         source ~/emsdk/emsdk_env.sh
     - On macOS also ensure: xcode-select --install ; brew install cmake
     - Then: bash build_wasm.sh
       (it downloads + SHA-verifies the Opus 1.6.1 tarball and builds with
        DRED/Deep-PLC/OSCE enabled; outputs public/opus-wasm.{js,wasm})

3. Run the server:
     node server.js
   It prints "Opus WASM demo running → http://localhost:3000".
   Open that URL. mic access works on http://localhost (secure context).

4. Tell me the local URL and confirm the page loads. Do NOT commit or push
   anything. Do NOT hardcode or ask me to paste Vonage credentials into source
   — they go into the web form at runtime.

Notes:
 - The opus-wasm-demo folder ONLY exists on branch claude/dazzling-goldberg-cd6axh.
 - To use the demo I will provide a Vonage Video API Key, Session ID, and Token
   (generated by my own Vonage server/app) directly in the web UI.
```

---

## TL;DR (if you'd rather just run it yourself)

```bash
git clone https://github.com/czoli1976/opus.git
cd opus
git checkout claude/dazzling-goldberg-cd6axh
cd opus-wasm-demo
node server.js          # → http://localhost:3000
```

That's all you need to *run* it (WASM is committed). See `README.md` for the
optional rebuild steps.
